# ===========================================
# LifeCycle 环境变量配置
# ===========================================
# Docker 用户: 复制此文件为 .env，修改后运行 docker compose up -d
# Vercel 用户: 在 Vercel 项目设置中添加以下环境变量
# ===========================================

# ===========================================
# 数据库
# ===========================================
# 数据库供应商: "sqlite" (默认) | "turso"
# - sqlite: Docker 自托管部署，使用本地 SQLite 文件（自动迁移建表）
# - turso: Serverless 平台 (Vercel 等)，使用 Turso 远程数据库（启动时自动建表）
DATABASE_PROVIDER="sqlite"

# SQLite（Docker 部署，容器内路径，无需修改）
DATABASE_URL="file:./dev.db"

# Turso（仅 DATABASE_PROVIDER="turso" 时需要）
# 1. 在 https://turso.tech 创建数据库
# 2. 获取连接 URL 和 Auth Token
# 3. 填入以下变量，应用启动时会自动创建表结构
# TURSO_DATABASE_URL="libsql://your-database.turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# ===========================================
# 认证 (Better-Auth)
# ===========================================
# 认证密钥（至少 32 个字符，可用 openssl rand -base64 32 生成）
BETTER_AUTH_SECRET="your-secret-key-min-32-chars"
# 应用访问地址（必须与实际访问地址一致）
BETTER_AUTH_URL="http://localhost:3000"

# ===========================================
# 应用
# ===========================================
# 应用公开访问地址
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ===========================================
# 定时任务（过期检查 + Webhook 通知）
# ===========================================
# Cron API 鉴权密钥（通过 HTTP 触发时需要，可用 openssl rand -hex 16 生成）
CRON_SECRET="your-cron-secret"

# 是否启用内置定时调度器（Docker 默认 true，Vercel 请设为 false）
# CRON_ENABLED=true

# Cron 表达式（默认每天 09:00）
# CRON_SCHEDULE="0 9 * * *"

# Cron 时区
# CRON_TIMEZONE="Asia/Shanghai"

# ===========================================
# Docker 部署
# ===========================================
# 映射到宿主机的端口
APP_PORT=3000
