# ============================================
# LifeCycle 环境变量配置模板
# ============================================
# 使用方式:
#   cp .env.example .env.production
#   vim .env.production  # 修改密钥
#   docker compose --env-file .env.production up -d
#
# 说明: 域名/地址无需配置，应用会从请求中自动检测
# ============================================

# ---- 认证密钥（必改） ----
# 生成方式: openssl rand -base64 32
BETTER_AUTH_SECRET=请替换为随机密钥

# ---- 应用端口 ----
APP_PORT=3000

# ---- 数据库 ----
# sqlite（默认）: 使用本地 SQLite 文件，数据存储在 Docker Volume 中
# turso: 使用 Turso 远程数据库（适合 Serverless）
DATABASE_PROVIDER=sqlite

# Turso 配置（仅 DATABASE_PROVIDER=turso 时需要）
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# ---- 定时任务（过期提醒 Cron） ----
# 生成方式: openssl rand -hex 16
CRON_SECRET=请替换为随机密钥
CRON_ENABLED=true
# Cron 表达式，默认每天早上 9 点执行
CRON_SCHEDULE="0 9 * * *"
CRON_TIMEZONE=Asia/Shanghai
